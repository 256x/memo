Bug fixes and internal improvements.

- Deleting a memo now returns to the list immediately instead of waiting for Git sync
- Remote trash sync now runs in the background and survives leaving the screen
